package com.challenge.app

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.runtime.Composable

/**
 * TODO(candidate):
 *  - A search text field at the top, state hoisted from the ViewModel.
 *  - Render each UI state (loading / results / empty / error).
 *  - Keep composables stateless & preview-friendly where you can.
 *  - Collect state in a lifecycle-aware way.
 *
 * Build a list row showing repo name, description, and star count.
 */
@Composable
fun SearchScreen(viewModel: SearchViewModel) {
    Column(androidx.compose.ui.Modifier.fillMaxSize()) {
        // your UI here
    }
}
